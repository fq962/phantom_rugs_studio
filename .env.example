# Credenciales SMTP para el formulario de contacto (nodemailer).
# Copia este archivo a `.env` y rellena los valores reales.
# Datos del proveedor de correo de phantomrugsstudio.com
# (p. ej. Zoho: smtp.zoho.com / Google Workspace: smtp.gmail.com / cPanel: mail.phantomrugsstudio.com)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
